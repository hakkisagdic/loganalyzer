<Project Sdk="Microsoft.NET.Sdk">

  <!--
    Alarm motoru ve bildirim kanalları (T21, T22).

    Ayrı bir proje olmasının sebebi Replay ve Authoring'inkiyle aynı: birden çok
    katmana birden dokunuyor. Kural tanımı ve kanal yapılandırması kontrol
    düzleminde duruyor, değerlendirme ise sorgu kapısından (IScopedQuery) olay
    verisine gidiyor. İkisinden birine koymak yanlış yöne bir bağımlılık
    doğururdu — ControlPlane sorgu katmanını tanımamalı, Query de bildirim
    göndermeyi.

    Üçüncü bir sebep bu projeye özel: burası ürünün DIŞARIYA bağlandığı ilk
    parça (Slack, Teams, SMTP, webhook). Dış dünyaya açılan kod ile kapsam
    zorlayan kodun aynı derlemede olmaması, "hangi sınıf ağa çıkabiliyor"
    sorusunun cevabını tek bir proje sınırına indiriyor.
  -->

  <ItemGroup>
    <ProjectReference Include="..\Bizigo.Query\Bizigo.Query.csproj" />
  </ItemGroup>

  <ItemGroup>
    <PackageReference Include="Microsoft.Extensions.Hosting.Abstractions" />
    <PackageReference Include="Microsoft.Extensions.Logging.Abstractions" />
    <PackageReference Include="Microsoft.Extensions.Options" />
    <PackageReference Include="Microsoft.Extensions.Http" />
  </ItemGroup>

</Project>
