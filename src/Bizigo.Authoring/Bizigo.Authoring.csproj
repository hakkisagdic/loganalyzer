<Project Sdk="Microsoft.NET.Sdk">

  <!--
    Parser yazarlığı: taslak deposu, yayın kapıları ve katalog beslemesi (T18).

    Ayrı bir proje olmasının sebebi Replay'inkiyle aynı: iki katmana birden
    dokunuyor. Kontrol düzlemi taslakları saklıyor, parser motoru onları
    derliyor ve sınıyor. İkisinden birine koymak yanlış yöne bir bağımlılık
    doğururdu — ControlPlane parser motorunu tanımamalı, Parsing de veritabanını.
  -->

  <ItemGroup>
    <ProjectReference Include="..\Bizigo.ControlPlane\Bizigo.ControlPlane.csproj" />
    <ProjectReference Include="..\Bizigo.Parsing\Bizigo.Parsing.csproj" />
  </ItemGroup>

  <ItemGroup>
    <PackageReference Include="Microsoft.Extensions.Logging.Abstractions" />
    <PackageReference Include="Microsoft.Extensions.Options" />
  </ItemGroup>

</Project>
