<Project Sdk="Microsoft.NET.Sdk">

  <!--
    M04 — `bizigo` yüzeyinin OKUMA araçları.

    NEDEN AYRI BİR PROJE. `Bizigo.Mcp` protokolü konuşuyor ve ürünü BİLMİYOR
    (gerekçe onun csproj yorumunda). Araçlar ürünü bilmek zorunda, dolayısıyla
    çekirdeğin ALTINDA değil YANINDA duruyorlar: bu proje çekirdeğe ve ürün
    katmanlarına bakıyor, çekirdek buraya hiç bakmıyor.

    NEDEN `Bizigo.Api` DEĞİL. Api kompozisyon kökü; araçları oraya koymak stdio
    taşımasını (`bizigo mcp serve`) ASP.NET'e bağlamak olurdu — `Bizigo.Mcp`
    csproj'unda ölçülmüş olan hata. Buradaki referans kümesi bilerek
    `Bizigo.Cli`'nin bugün taşıdığı kümenin ALT kümesi, yani CLI'yi hiçbir yeni
    çatıya sürüklemiyor:

      Cli bugün : Query · Ingest · Parsing · Alerting · Mcp · Simulators
      burada    : Query ·          Parsing · Alerting · Mcp

    `Bizigo.Authoring` BİLEREK YOK. `catalog.parsers`'ın ikinci ucu
    (`/v1/parsers/coverage`) `CatalogCoverageCache`'i istiyor, o da Authoring'de
    — ve `Measure(...)` katalog değiştiyse BÜTÜN KATALOĞU yeniden ölçüyor.
    Modelin göremediği bir maliyeti modelin tetikleyebilmesi istenmedi; kapsam
    oranı bu araçta yok, gerekçesi `CatalogParsersTool` belgesinde.

    M07 · EVIDENCE VE RCA EKLENDİ, VE MALİYETİ ÖLÇÜLDÜ.

    Kaynaklar (`Resources/`) kanıt paketi ve RCA raporu belgelerini sunuyor,
    yani `EvidenceBundleStore` ve `RcaReportStore` gerekiyor. Bu, yukarıdaki
    "Cli'nin alt kümesi" kuralını genişletiyor — bilerek, ve ölçülen maliyetle:

      Bizigo.Evidence → Contracts · Query                       (Cli'de İKİSİ DE var)
      Bizigo.Rca      → Contracts · ControlPlane · ScenarioPlugin · Extensions.Http

    Yeni çatı YOK; hepsi ürünün kendi katmanları ve Cli'nin bugünkü kümesi
    ControlPlane'i (Ingest) ve Http'yi (Alerting) zaten taşıyor.

    ALTERNATİF ELENDİ: kaynakları ayrı bir derlemeye koyup yalnızca
    `Bizigo.Api`'den referans vermek. O zaman stdio taşıması (`bizigo mcp serve`)
    bu kaynakları HİÇ göremezdi, yani aynı yüzey iki taşımada FARKLI şey sunardı
    — M01'in "iki taşıma, aynı küme" iddiasının tam tersi, ve ayrışmayı
    gösterecek hiçbir bekçi olmazdı.
  -->

  <ItemGroup>
    <ProjectReference Include="..\Bizigo.Mcp\Bizigo.Mcp.csproj" />
    <ProjectReference Include="..\Bizigo.Query\Bizigo.Query.csproj" />
    <ProjectReference Include="..\Bizigo.Parsing\Bizigo.Parsing.csproj" />
    <ProjectReference Include="..\Bizigo.Alerting\Bizigo.Alerting.csproj" />
    <!--
      M10 — `rca.quality`'nin sayaçları (`GoldenReviewStore`).

      REFERANS KÜMESİ HÂLÂ CLI'NİN ALT KÜMESİ DEĞİL, ve bu ölçüldü: `Bizigo.Cli`
      bugün `Bizigo.Evidence`'a referans TAŞIMIYOR. Yani yukarıdaki tablo bu
      satırla bozuluyor ve bozulmasının bedeli açıkça şu: ürün yüzeyinin stdio
      süreci (`bizigo mcp serve`) artık Evidence derlemesini de yüklüyor.

      Bedel ÖLÇÜLDÜ ve küçük: Evidence'ın kendi referansları
      `Bizigo.Contracts` + `Bizigo.Query`, ikisi de CLI'de ZATEN var, ve paket
      olarak yalnızca DI/Logging soyutlamaları geliyor. Yeni bir çatı, yeni bir
      sürücü, yeni bir ağ bağımlılığı YOK — Evidence'ın kendi csproj yorumu
      ClickHouse.Driver bağımlılığını mimari testle yasaklıyor.

      Alternatif `rca.quality`'yi ayrı bir derlemeye koymaktı ve daha pahalı:
      ikinci bir araç derlemesi, `McpEndpoints.ToolAssemblies` ile
      `McpCommandHandlers.ToolAssemblies` listelerinin İKİSİNE de yazılması
      gereken ikinci bir satır demek — ve M04'ün ölçtüğü budama körlüğünün
      yeni bir yüzeyi.
    -->
    <ProjectReference Include="..\Bizigo.Evidence\Bizigo.Evidence.csproj" />

    <!--
      M07 — kaynakların deposu (`RcaReportStore`, `AddBizigoRcaTriggers` içinde).
      Yukarıdaki gerekçenin aynısı geçerli: `Bizigo.Rca`'nın kendi referansları
      `Bizigo.Contracts` + `Bizigo.ControlPlane` ve ikisi de CLI'de zaten var.
    -->
    <ProjectReference Include="..\Bizigo.Rca\Bizigo.Rca.csproj" />
  </ItemGroup>

  <ItemGroup>
    <!--
      Araç gövdeleri `internal protected` bir aşırı yüklemeden koşuyor
      (`ProductReadTool.ExecuteScopedAsync`) ve birim testleri onu DOĞRUDAN
      çağırıyor: kapsam bağlanması M08'i beklerken bile gerçek gövde ölçülüyor.
      Bu satır olmadan testler yalnızca `SampleAsync`'i görürdü — yani şemayı
      ölçen ama mantığı ölçmeyen bir paket.
    -->
    <InternalsVisibleTo Include="Bizigo.UnitTests" />
    <InternalsVisibleTo Include="Bizigo.IntegrationTests" />
  </ItemGroup>

</Project>
