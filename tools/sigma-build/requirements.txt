# Derleme hattının bağımlılıkları.
#
# ⚠️ SÜRÜMLER SİDECAR'INKİLERLE AYNI OLMAK ZORUNDA ve bunu bir test tutuyor
#    (`test_requirements.py`). Gerekçe: sidecar'ın `/v1/sigma/compile` ucu
#    UI'daki "bu kuralı derle ve önizle" akışını besliyor. İki taraf ayrı sürüm
#    sabitlerse ekranın gösterdiği SQL ile build-time üretilen SQL ayrışır — ve
#    **ayrıştıklarında hiçbir şey kırmızı yanmaz.**
#
# Neden `-r ../../sidecar/requirements.txt` DEĞİL: o dosya drain3'ü bir git
# SHA'sından çekiyor ve Sigma derleme hattını üçüncü taraf bir deponun
# erişilebilirliğine bağlamak olurdu (`ci.yml`, sidecar işinin notu). F3 bu hatta
# bağlı; ağ, kapının gerekçesi olamaz.
#
# Yani: tek kaynak yerine **iki dosya + bir bekçi**. Kopya değil, ölçülen kopya.
pySigma==1.5.0
pysigma-backend-clickhouse==1.1.1
PyYAML==6.0.3
