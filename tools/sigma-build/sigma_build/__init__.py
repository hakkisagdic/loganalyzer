"""Sigma derleme hattı — **derleme zamanı** aracı (T32).

Sıcak yolda değil; sidecar servisi de değil. `sidecar/app/` çalışan bir servis,
burası bir yapı aracı. İkisi aynı pySigma sürümlerini kullanıyor
(`requirements.txt` doğrudan `sidecar/requirements.txt`'e işaret ediyor) çünkü
derleme, üretimde kurulacak olandan başka bir şeyi derlememeli.
"""
