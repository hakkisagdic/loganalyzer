node_modules/
.next/
out/
next-env.d.ts
*.tsbuildinfo

# Yerel ortam. `.env.example` commitleniyor, bu dosya asla.
.env
.env.local
.env*.local

# Ekran görüntüsü ara dosyaları (afterAll siliyor; ağ kesilirse kalabilir)
.screenshots-tmp/

# Playwright'ın koşum çıktısı: iz, video ve hata bağlamı. Yalnızca DÜŞEN
# koşumda doluyor (`retain-on-failure`) ve teşhis içindir — ekran görüntüleri
# buraya değil `docs/ekran-goruntuleri/uctan-uca/` altına yazılıyor, onlar işin
# ürünü ve commitleniyor.
.playwright/
