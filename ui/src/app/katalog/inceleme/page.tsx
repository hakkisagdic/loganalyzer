import { ReviewQueue } from "./ReviewQueue";

/** İnceleme kuyruğu: yayın bekleyen taslaklar, fark görünümü, onay/geri gönderme (T20). */
export default function ReviewPage() {
  return <ReviewQueue />;
}
