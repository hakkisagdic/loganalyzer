import { CatalogOverview } from "./CatalogOverview";

/** Parser kataloğu: liste, kapsam ölçüsü, GROK003 uyarısı, sürüm geçmişi (T20). */
export default function CatalogPage() {
  return <CatalogOverview />;
}
