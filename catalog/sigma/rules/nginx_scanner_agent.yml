title: "nginx: tarayıcı user-agent"
id: 06977082-0000-4000-8000-000000000000
status: experimental
description: T30 ölçüm örneklemi — F1 kataloğunun tanıdığı vendor'lara göre yazıldı.
logsource:
  category: firewall
  product: nginx
detection:
  selection:
    user_agent|contains:
      - 'sqlmap'
      - 'nikto'
  condition: selection
level: medium
