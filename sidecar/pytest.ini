[pytest]
testpaths = tests
# conftest.py sys.path'e `sidecar/` ekliyor; `app` paketi oradan import ediliyor.
addopts = -q
filterwarnings =
    # jsonpickle/drain3 eski Python API'lerine dokunuyor; uyarı gürültüsü
    # gerçek hatayı gizliyor.
    ignore::DeprecationWarning
